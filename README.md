# InputStream Processor Core

A tiny, dependency-free Java core for incremental item processing from `InputStream`.

The core is format-neutral and has no third-party runtime dependencies.

Maven coordinate: `io.github.j-util:inputstream-processor-core`
